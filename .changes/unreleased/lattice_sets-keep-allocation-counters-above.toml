package = "lattice_sets"
kind = "Fixed"
body = """
Keep allocation counters above pruning history

ORSet merge and prune retain a counter floor from the pruning frontier. Generic round trips no longer change valid allocator metadata, and a rebound writer can add values above retained pruning clocks."""
