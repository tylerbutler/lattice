package = "lattice_sequence"
kind = "Fixed"
body = """
Restore allocation counters from retained history

Decoding preserves the allocation high-water mark from item, operation, origin, block, forwarding, and frontier metadata even when the serialized counter understates it. Subsequent edits do not reuse identifiers still present in the snapshot."""
