package = "lattice_registers"
kind = "Breaking"
body = """
Require `replica_id` in version 2 LWWRegister snapshots

Snapshot producers must store the winning writer ID. Version 1 snapshots still use the legacy empty ID. Applications must define how to migrate incomplete version 2 snapshots."""
