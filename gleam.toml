name = "my_gleam_project"
version = "0.1.0"
description = "A Gleam project"
licences = ["MIT"]
repository = { type = "github", user = "YOUR_USERNAME", repo = "my_gleam_project" }
gleam = ">= 1.7.0"

# Uncomment to mark internal modules (not exported in public API):
# internal_modules = ["my_gleam_project/internal", "my_gleam_project/internal/*"]

[dependencies]
gleam_stdlib = ">= 0.48.0 and < 2.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
