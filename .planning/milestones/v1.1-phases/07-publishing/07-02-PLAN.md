---
phase: 07-publishing
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified: []
autonomous: false
requirements:
  - PUB-04
user_setup:
  - service: hex.pm
    why: "Package registry for Gleam/Erlang ecosystem"
    env_vars:
      - name: HEXPM_API_KEY
        source: "hex.pm -> Account Settings -> API Keys -> Generate"
    dashboard_config: []

must_haves:
  truths:
    - "gleam build succeeds on both targets"
    - "gleam docs build succeeds without warnings"
    - "All 228+ tests pass on both Erlang and JavaScript targets"
    - "No boilerplate placeholders remain in any file"
    - "Package is published to Hex.pm"
  artifacts:
    - path: "gleam.toml"
      provides: "Verified metadata ready for publish"
      contains: "version = \"1.1.0\""
  key_links:
    - from: "gleam.toml"
      to: "hex.pm/packages/lattice"
      via: "gleam publish"
      pattern: "name = \"lattice\""
---

<objective>
Verify the package is publish-ready, then publish lattice v1.1.0 to Hex.pm.

Purpose: PUB-04 requires the package to be successfully published. This plan runs the full verification suite and then publishes.
Output: Package live on Hex.pm at https://hex.pm/packages/lattice
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-publishing/07-RESEARCH.md
@.planning/phases/07-publishing/07-01-SUMMARY.md

@gleam.toml
@README.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full pre-publish verification</name>
  <files></files>
  <action>
Run the complete pre-publish checklist. No files are modified -- this is verification only.

1. Build verification:
   - `gleam build` succeeds
   - `gleam test --target erlang` passes all tests
   - `gleam test --target javascript` passes all tests
   - `gleam docs build` succeeds without warnings

2. Metadata verification:
   - `gleam.toml` has: name, version (1.1.0), description, licences, repository
   - No placeholder values remain: `rg 'my_gleam_project|YOUR_NAME' src/ README.md LICENSE gleam.toml CHANGELOG.md`

3. Content verification:
   - README.md contains: Installation section, Quickstart section, type overview
   - CHANGELOG.md contains: v1.1.0 entry
   - LICENSE contains: real copyright holder (not YOUR_NAME)

If any check fails, report the specific failure. Do NOT proceed to the publish checkpoint until all checks pass.
  </action>
  <verify>
    <automated>gleam build && gleam test --target erlang && gleam test --target javascript && gleam docs build 2>&1 | tail -1 && echo "ALL CHECKS PASS"</automated>
  </verify>
  <done>All builds pass on both targets, all 228+ tests pass, docs build clean, no boilerplate in any file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Review and publish to Hex.pm</name>
  <files></files>
  <action>
Present the pre-publish verification results to the user and provide publish instructions. This is a human checkpoint -- the user must review the package and initiate the publish.

What was built (Plan 01 + Task 1 verification):
- Complete package metadata in gleam.toml (v1.1.0, description, repository, licences)
- README.md with installation, quickstart, and type overview
- CHANGELOG.md with v1.1.0 entries via changie
- LICENSE with real copyright holder
- All builds and tests pass on both Erlang and JavaScript targets
  </action>
  <verify>User confirms package is published by typing "published"</verify>
  <done>Package is live on Hex.pm at https://hex.pm/packages/lattice</done>
  <how-to-verify>
1. Review README.md -- does it accurately represent the library?
2. Review CHANGELOG.md -- are the v1.1.0 entries correct?
3. Check gleam.toml version is 1.1.0
4. Publish the package:
   - **Option A (manual):** `HEXPM_API_KEY=your_key gleam publish --yes`
   - **Option B (CI):** Push a `v1.1.0` tag which triggers the publish.yml workflow:
     ```bash
     git tag v1.1.0
     git push origin v1.1.0
     ```
   - Ensure `HEXPM_API_KEY` secret is configured in GitHub repo settings if using CI path
5. After publish, verify at: https://hex.pm/packages/lattice
  </how-to-verify>
  <resume-signal>Type "published" once the package is live on Hex.pm, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `gleam build` succeeds
- `gleam test --target erlang` and `gleam test --target javascript` both pass
- `gleam docs build` succeeds
- Package appears at https://hex.pm/packages/lattice (post-publish)
</verification>

<success_criteria>
- All pre-publish checks pass (builds, tests, docs, no boilerplate)
- Package published to Hex.pm and accessible at https://hex.pm/packages/lattice
- v1.1 milestone is complete
</success_criteria>

<output>
After completion, create `.planning/phases/07-publishing/07-02-SUMMARY.md`
</output>
